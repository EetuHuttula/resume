/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/EetuHcv',
  assetPrefix: '/EetuHcv/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
